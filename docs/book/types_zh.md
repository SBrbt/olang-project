# 类型系统

**[English](types.md)** | **中文**

---

### 内置类型

| 类型 | 大小 | 说明 |
|------|------|------|
| `void` | 0 | 无值 / 无返回值 |
| `bool` | 1 字节 | 布尔 |
| `i8`, `i16`, `i32`, `i64` | 1 / 2 / 4 / 8 字节 | 有符号整数 |
| `u8`, `u16`, `u32`, `u64` | 1 / 2 / 4 / 8 字节 | 无符号整数 |
| `f16`, `f32`, `f64` | 2 / 4 / 8 字节 | 浮点（IEEE binary16/32/64） |
| `b8`, `b16`, `b32`, `b64` | 1 / 2 / 4 / 8 字节 | 定宽原始位向量 |
| `ptr` | 8 字节 | 裸指针 |

### 字面量后缀

**整数：** 可选后缀 `i8`、`i16`、`i32`、`i64`、`u8`、`u16`、`u32`、`u64`、`b8`、`b16`、`b32`、`b64`。无后缀的十进制字面量默认为 `i32`（会做范围检查）。

**浮点：** 十进制，可有小数部分和/或指数；可选后缀 `f16`、`f32`、`f64`。无后缀时字面量类型为 `f64`。

```olang
42           // i32
42i64        // i64
42u64        // u64
255b8        // b8
3.14f64      // f64
18446744073709551615u64  // UINT64_MAX
```

### 类型定义

#### 结构体

```olang
type Point = struct { x: i32, y: i32 };
type Rect = struct { tl: Point, br: Point };
```

- 字段顺序声明
- 支持嵌套
- 赋值使用值拷贝

#### 数组

```olang
type IntArray = array<i32, 10>;
```

- 编译期确定大小
- 变量实际为指针
- 不检查越界

### 类型转换

#### `cast<目标类型>(表达式)`

在**不同类型**之间做编译器允许的显式转换：同一整数族内的变宽/截断（`i*`↔`i*`、`u*`↔`u*`）、`b*`↔`b*`、`f*`↔`f*`、`bool`↔整数、浮点↔整数等。源类型与目标类型相同时不能用 `cast`（同宽「换类型标签」请用 `reinterpret`）。

```olang
cast<i32>(i64_value)   // 截断
cast<i32>(i8_value)    // 扩展
cast<u32>(200u8)       // 无符号变宽
```

#### `reinterpret<目标类型>(表达式)`

在**相同字节宽度**下把值的类型重新解释为另一类型（例如 `i32`↔`u32`、`u64`↔`ptr`、`b32`↔`i32`）。不能用于 `bool`，也不能用于结构体或数组。

```olang
reinterpret<u32>(-1i32)      // 4 字节不变，按无符号看
reinterpret<ptr>(0x1000u64)    // 8 字节整数与指针互释
```

### 类型分类

| 分类 | 类型 | 初始化要求 |
|------|------|-----------|
| 标量 | 除 `void` 外的内置类型 | `let` 时必须初始化 |
| 聚合 | struct、array | 可延后初始化 |

---

[返回文档](../README_zh.md)
